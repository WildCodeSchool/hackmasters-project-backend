# ===============================
# = DATA SOURCE
# ===============================
# Tell Spring to use a different environment-specific property file
spring.profiles.active=@spring.profiles.active@
# Set here configurations for the database connection
# Connection url for the database "dbname"
spring.datasource.url=jdbc:postgresql://localhost:5432/chef_compass

# usersname and password
spring.datasource.username=
spring.datasource.password=

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.tomcat.test-while-idle=true
spring.datasource.tomcat.validation-query=SELECT 1

# ===============================
# = SPRING DATA JPA
# ===============================
# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Synchronize Hibernate time zone with MySQL
spring.jpa.properties.hibernate.jdbc.time_zone=Europe/Paris

# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto=update
server.port=3000
spring.jpa.open-in-view=false
spring.main.allow-circular-references=true
your.jwt.secret=pHrtq0A+sBj06U/WjJztt07uUSnPhtLycZYCTXJQgEw=
